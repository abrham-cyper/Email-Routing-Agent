name: llm-project
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - torchaudio
  - pip
  - pip:
    - transformers
    - datasets
    - peft
    - accelerate
    - scikit-learn
    - tqdm
    - jupyter
    - matplotlib
    - evaluation # Assuming evaluation metrics are needed, if not standard sklearn metrics are sufficient
